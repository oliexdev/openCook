<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Downloaded update APKs live in cacheDir/updates and are shared with the
         system package installer via the FileProvider. -->
    <cache-path name="updates" path="updates/" />
</paths>
